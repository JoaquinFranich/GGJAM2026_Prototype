# 🏗️ DIAGRAMA DE JERARQUÍA DEL PROYECTO

## 📊 ESTRUCTURA COMPLETA

```
┌─────────────────────────────────────────────────────────────┐
│                    PROYECTO GODOT                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              │
        ┌─────────────────────┴─────────────────────┐
        │                                           │
        ▼                                           ▼
┌──────────────────┐                    ┌──────────────────────┐
│   AUTOLOADS      │                    │   ESCENAS DEL JUEGO  │
│  (Singletons)    │                    │   (Se cargan según   │
│                  │                    │    navegación)        │
└──────────────────┘                    └──────────────────────┘
        │                                           │
        │                                           │
   ┌────┴────┬──────────────┬──────────┐          │
   │         │              │          │          │
   ▼         ▼              ▼          ▼          ▼
┌──────┐ ┌──────────┐ ┌─────────┐ ┌────────┐ ┌──────────┐
│Scene │ │Dialogue │ │UI_      │ │test_   │ │test_     │
│Mgr   │ │Manager  │ │manager  │ │node_00 │ │node_A    │
└──────┘ └──────────┘ └─────────┘ └────────┘ └──────────┘
                              │
                              │
                    ┌─────────┴─────────┐
                    │                   │
                    ▼                   ▼
            ┌──────────────┐   ┌──────────────┐
            │ CanvasLayer   │   │ FocusItem    │
            │ (layer = 5)   │   │ Container    │
            │               │   │ (Node2D)     │
            └───────────────┘   └──────────────┘
                    │                   │
        ┌───────────┼───────────┐       │
        │           │           │       │
        ▼           ▼           ▼       ▼
┌──────────┐ ┌──────────┐ ┌────────┐ ┌──────┐
│Direction │ │Dialogue │ │Inventory│ │Focus │
│Buttons   │ │Panel    │ │Panel    │ │Item  │
│          │ │         │ │(oculto) │ │(din.)│
└──────────┘ └──────────┘ └────────┘ └──────┘
    │
    ├── ButtonLeft
    ├── ButtonRight
    ├── ButtonUp
    └── ButtonDown
```

---

## 🎯 FLUJO DE FUNCIONAMIENTO

### 1️⃣ Al iniciar el juego:

```
1. Godot carga los AUTOLOADS (singletons)
   ├── SceneManager se crea
   ├── DialogueManager se crea
   └── UI_manager se crea (con todos sus botones)

2. Godot carga la escena inicial (test_node_A.tscn)
   └── El nodo raíz tiene scene_ui_config.gd
       └── En _ready() llama a UI_manager.configure_scene_ui()
           ├── Crea FocusItem en la posición indicada
           └── Muestra/oculta botones según configuración
```

### 2️⃣ Al cambiar de escena:

```
1. SceneManager.change_scene() se ejecuta
   ├── Hace fade out
   ├── Cambia la escena
   └── Hace fade in

2. La nueva escena se carga
   └── Su nodo raíz tiene scene_ui_config.gd
       └── En _ready() configura UI_manager de nuevo
           ├── Limpia el FocusItem anterior
           ├── Crea nuevo FocusItem en nueva posición
           └── Actualiza botones visibles
```

---

## 🔄 CICLO DE VIDA DE UNA ESCENA

```
┌─────────────────────────────────────────┐
│  ESCENA SE CARGA                        │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  _ready() se ejecuta                     │
│  └── scene_ui_config.gd                 │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  UI_manager.configure_scene_ui()        │
│  ├── Limpia FocusItem anterior         │
│  ├── Crea nuevo FocusItem              │
│  └── Muestra/oculta botones            │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  ESCENA LISTA                           │
│  - Background visible                   │
│  - FocusItem en posición                │
│  - Botones configurados                 │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  JUGADOR INTERACTÚA                     │
│  - Click en FocusItem → avanza         │
│  - Click en botón → navega             │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  SceneManager cambia de escena          │
│  └── Se repite el ciclo                 │
└─────────────────────────────────────────┘
```

---

## 📍 POSICIONAMIENTO VISUAL

```
┌─────────────────────────────────────────────┐
│          PANTALLA DEL JUEGO                 │
│         (1920 x 1080)                       │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  UI_manager (CanvasLayer layer 5)   │   │
│  │  ┌───────────────────────────────┐ │   │
│  │  │ ButtonUp (arriba centro)      │ │   │
│  │  └───────────────────────────────┘ │   │
│  │                                     │   │
│  │  ┌───┐                    ┌───┐   │   │
│  │  │ ← │                    │ → │   │   │
│  │  │Lft│                    │Rgt│   │   │
│  │  └───┘                    └───┘   │   │
│  │                                     │   │
│  │  ┌───────────────────────────────┐ │   │
│  │  │ ButtonDown (abajo centro)     │ │   │
│  │  └───────────────────────────────┘ │   │
│  │                                     │   │
│  │  ┌──────────┐                       │   │
│  │  │FocusItem │ (posición dinámica)   │   │
│  │  └──────────┘                       │   │
│  │                                     │   │
│  │  ┌───────────────────────────────┐ │   │
│  │  │ DialoguePanel (abajo)         │ │   │
│  │  └───────────────────────────────┘ │   │
│  │                                     │   │
│  │  ┌──────┐                          │   │
│  │  │Invent│ (lateral derecho, oculto) │   │
│  │  └──────┘                          │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  CONTENIDO DE LA ESCENA            │   │
│  │  (Background, sprites, etc.)       │   │
│  └─────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```

**IMPORTANTE:** La UI está SIEMPRE por delante porque usa `CanvasLayer`.

---

## ✅ CHECKLIST PARA CADA ESCENA

Cuando crees o modifiques una escena, verifica:

- [ ] El nodo raíz tiene el script `scene_ui_config.gd`
- [ ] `focus_item_position` está configurado correctamente
- [ ] `visible_direction_buttons` tiene los botones que necesitas
- [ ] NO hay instancias de `UI_manager` en la escena
- [ ] NO hay instancias de `FocusItem` en la escena
- [ ] NO hay botones de dirección en la escena
- [ ] Solo hay el contenido visual (Background, sprites, etc.)

---

## 🎓 CONCEPTOS CLAVE

### Singleton (Autoload)
- Se crea UNA VEZ al inicio
- Existe durante TODO el juego
- Accesible desde cualquier escena
- NO se destruye al cambiar de escena

### CanvasLayer
- Permite que la UI esté siempre por delante
- No se ve afectado por la cámara
- Perfecto para interfaces de usuario

### Configuración por Escena
- Cada escena configura SU propia UI
- El FocusItem cambia de posición según la escena
- Los botones se muestran/ocultan según la escena
- Todo se hace automáticamente con `scene_ui_config.gd`
